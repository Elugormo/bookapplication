import React, { PureComponent } from 'react';
import { compose, gra phql } from 'react-apollo'


class S extends Component {
    constructor(props) {
        super(props);
        this.state = {  }
    }
    render() { 
        return (  );
    }
}
 
export default compose()(S);